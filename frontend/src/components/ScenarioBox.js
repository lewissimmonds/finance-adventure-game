import React from "react";

const ScenarioBox = ({ scenarioText }) => (
  <div>
    <p>{scenarioText}</p>
    {/* Similar to QuestionBox, expand this component as needed */}
  </div>
);

export default ScenarioBox;
