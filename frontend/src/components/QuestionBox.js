import React from "react";

const QuestionBox = ({ questionText }) => (
  <div>
    <p>{questionText}</p>
    {/* You can add more structure to this component as needed */}
  </div>
);

export default QuestionBox;
